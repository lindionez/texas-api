{
    "function_para_baixar_url": "const axios = require('axios');      const https = require('https');               const httpsAgent = new https.Agent({ rejectUnauthorized: false,});           function getBuffer(url) {  return axios.get(url, {  httpsAgent,  responseType: 'arraybuffer'  }).then(response => Buffer.from(response.data, 'binary'))}"
}